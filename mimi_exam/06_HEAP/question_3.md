# LeetCode 295. Find Median from Data Stream

Implement a class `MedianFinder` to add numbers and calculate the running median of a data stream.

## Example 1:
**Input:**
```cpp
MedianFinder* m = new MedianFinder();
m->addNum(1);
m->addNum(2);
m->findMedian(); // return 1.5
```
