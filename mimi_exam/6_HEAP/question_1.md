# LeetCode Style: Verify Max-Heap Structure

Verify if a given complete binary tree represented as an array satisfies Max-Heap properties.

## Example 1:
**Input:** `arr = [90, 15, 10, 7]`
**Output:** `true`
